export const STORAGE_KEYS = {
  SESSIONS: 'legal_rag_sessions',
  ACTIVE_SESSION_ID: 'legal_rag_active_session_id',
} as const;

export const ACCEPTED_FILE_TYPES = '.pdf,.docx,.doc,.txt';
export const MAX_FILE_SIZE_MB = 10;
